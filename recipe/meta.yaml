{% set name = "openff-nagl" %}
{% set version = "0.2.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/openforcefield/openff-nagl/archive/v{{ version }}.tar.gz
  sha256: 681ae07a6e3cc3d07f19e9c715f8170cc727b5212322667d7a79387b130c306f

build:
  noarch: python
  number: 1
  script: "{{ PYTHON }} -m pip install . -vv --no-deps"

requirements:
  host:
    - python >=3.9
    - pip
    - versioneer-518
  run:
    - python >=3.9
    - click
    - click-option-group
    - tqdm
    - openff-toolkit-base >=0.11.0
    - openff-units
    - pydantic
    - sqlalchemy
    - sqlite
    - pytorch
    - pytorch-lightning
    - rich


test:
  imports:
    - openff.nagl

about:
  home: https://openforcefield.org/
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: A library for applying graph convolutional neural networks to molecules
  dev_url: https://github.com/openforcefield/openff-nagl

extra:
  recipe-maintainers:
    - lilyminium
